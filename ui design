import React from 'react';
import { View, Text, TextInput, ScrollView, TouchableOpacity, Image } from 'react-react-native';
import { 
  Bell, Search, MapPin, Home, ClipboardList, Wallet, User, 
  Wrench, Scissors, Zap, Car, ShieldAlert, Paintbrush, Hammer 
} from 'lucide-react-native';

const CATEGORIES = [
  { id: 1, name: 'Home Repair', icon: Wrench },
  { id: 2, name: 'Salon & Care', icon: Scissors },
  { id: 3, name: 'Electrical', icon: Zap },
  { id: 4, name: 'Automotive', icon: Car },
  { id: 5, name: 'Pest Control', icon: ShieldAlert },
  { id: 6, name: 'Painting', icon: Paintbrush },
  { id: 7, name: 'Carpentry', icon: Hammer },
];

export default function OneUIScreen() {
  return (
    <View className="flex-1 bg-[#F4F5F7]">
      {/* Scrollable Viewing & Interaction Area */}
      <ScrollView className="flex-1 px-5 pt-12" showsVerticalScrollIndicator={false}>
        
        {/* Top Header Section */}
        <View className="flex-row justify-between items-center mb-2">
          <Text className="text-2xl font-bold text-[#1C1C1E]">Good Evening, Apporio</Text>
          <TouchableOpacity className="p-2.5 bg-white rounded-full shadow-sm">
            <Bell size={20} color="#1C1C1E" />
          </TouchableOpacity>
        </View>

        {/* Location Badge */}
        <View className="flex-row items-center mb-6">
          <MapPin size={14} color="#0066FF" />
          <Text className="ml-1 text-xs text-[#8E8E93] font-medium" numberOfLines={1}>
            ITHUM TOWER, Block A, Industrial Area, Sector...
          </Text>
        </View>

        {/* One UI Search Bar (Pill Shape) */}
        <View className="flex-row items-center bg-white px-4 py-3 rounded-full shadow-sm mb-6">
          <Search size={18} color="#8E8E93" />
          <TextInput 
            placeholder="Search for a service..." 
            placeholderTextColor="#8E8E93"
            className="ml-3 flex-1 text-sm text-[#1C1C1E]"
          />
        </View>

        {/* Promotional Banner (Squircle Container) */}
        <View className="bg-[#0066FF] rounded-3xl p-5 mb-8 shadow-sm flex-row justify-between items-center">
          <View className="flex-1 pr-3">
            <Text className="text-white text-xs font-semibold uppercase tracking-wider mb-1 opacity-80">
              Special Offer
            </Text>
            <Text className="text-white text-lg font-bold mb-3">
              Tools & Home Services
            </Text>
            <TouchableOpacity className="bg-white px-4 py-2 rounded-full self-start">
              <Text className="text-[#0066FF] text-xs font-bold">Book Now</Text>
            </TouchableOpacity>
          </View>
        </View>

        {/* Categories Section */}
        <Text className="text-lg font-bold text-[#1C1C1E] mb-4">Handyman Services</Text>

        {/* 2-Column Grid with Squircle Cards */}
        <View className="flex-row flex-wrap justify-between pb-24">
          {CATEGORIES.map((item) => {
            const IconComponent = item.icon;
            return (
              <TouchableOpacity 
                key={item.id}
                className="w-[48%] bg-white p-4 rounded-3xl mb-4 items-center shadow-sm active:opacity-70"
              >
                <View className="w-12 h-12 rounded-2xl bg-[#F0F4FF] justify-center items-center mb-3">
                  <IconComponent size={24} color="#0066FF" />
                </View>
                <Text className="text-sm font-semibold text-[#1C1C1E] text-center">
                  {item.name}
                </Text>
              </TouchableOpacity>
            );
          })}
        </View>
      </ScrollView>

      {/* Floating One UI Bottom Navigation */}
      <View className="absolute bottom-0 left-0 right-0 bg-white/95 border-t border-gray-100 px-6 py-3 flex-row justify-around items-center">
        <TouchableOpacity className="items-center">
          <View className="bg-[#E5F0FF] px-4 py-1.5 rounded-full mb-1">
            <Home size={20} color="#0066FF" />
          </View>
          <Text className="text-xs font-bold text-[#0066FF]">Home</Text>
        </TouchableOpacity>

        <TouchableOpacity className="items-center opacity-50">
          <ClipboardList size={20} color="#1C1C1E" />
          <Text className="text-xs text-[#1C1C1E] mt-1">Orders</Text>
        </TouchableOpacity>

        <TouchableOpacity className="items-center opacity-50">
          <Wallet size={20} color="#1C1C1E" />
          <Text className="text-xs text-[#1C1C1E] mt-1">Wallet</Text>
        </TouchableOpacity>

        <TouchableOpacity className="items-center opacity-50">
          <User size={20} color="#1C1C1E" />
          <Text className="text-xs text-[#1C1C1E] mt-1">Settings</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}